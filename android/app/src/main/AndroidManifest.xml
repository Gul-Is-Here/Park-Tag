<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Android 13+ requires this to show any notification, including FCM
         chat-message pushes (FR-05/FR-06). firebase_messaging's
         requestPermission() call handles the runtime prompt. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="parktag_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Keeps the Flutter engine from also trying to parse an
                 incoming deep link into an initial route on its own — this
                 app decides routing itself (app_links + SplashController),
                 not Navigator 2.0/go_router. -->
            <meta-data android:name="flutter_deeplinking_enabled" android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- FR-09: the public Scan Contact Page's own URL
                 (https://car-ping-9d4f8.web.app/scan/{vehicleId}), registered
                 as a verified Android App Link — when this app is installed,
                 tapping that link (from a QR scan, share, or reopening it
                 after installing) opens straight into the app instead of a
                 browser; see DeepLinkService. autoVerify requires
                 /.well-known/assetlinks.json to be hosted on that domain
                 listing this app's package name + release SHA-256
                 fingerprint, or Android silently falls back to the browser. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="car-ping-9d4f8.web.app" android:pathPrefix="/scan"/>
            </intent-filter>
        </activity>
        <!-- FCM display configuration. Without these, a push that FCM
             reports as successfully delivered can still fail to appear:
             Android 8+ drops a notification whose channel does not exist,
             and with no icon the system flattens the launcher icon into an
             unrecognisable white square. The channel itself is created in
             MainActivity.kt and its id must match this value. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="parktag_messages" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
