# Copy to .env and fill in from your SMS gateway's API docs.
# Placeholders: {key} {to} {sender} {message}  ({to} is sent as 923XXXXXXXXX)
SMS_API_URL=https://your-gateway.pk/api/send?api_key={key}&to={to}&mask={sender}&message={message}
SMS_SENDER_ID=ParkTag
