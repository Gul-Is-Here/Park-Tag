# Placeholder values — SMS OTP gateway not yet configured.
# Replace with real values when ready (see .env.example).
SMS_API_URL=https://unconfigured-placeholder.invalid/send?api_key={key}&to={to}&mask={sender}&message={message}
SMS_SENDER_ID=ParkTag
